import type { NextPage } from 'next';
import PageTemplate from '../components/common/PageTemplate';

const AboutPage: NextPage = () => {
  return <PageTemplate right={false}>AboutPage</PageTemplate>;
};

export default AboutPage;
